# MyWebServer 项目面试问答

> 请在每个问题下方写出你的回答，完成后我会逐题评审并给出建议。

---

## 一、Bug 定位与代码审查（6 题）

### Q1
阅读 `webserver.h` 中 `dealwithclient()` 方法（约第 179-203 行）。这段代码中 LT/ET 触发模式的 accept 逻辑是否存在问题？如果有，请指出具体是什么问题。

---

（你的回答）

---

---

### Q2
在 `main.cpp` 中，`server.init()` 被调用后，`setTrigMode()` 是否会被执行？如果用户通过命令行传入 `-m 3`（ET + ET），实际生效的触发模式是什么？为什么？

---

（你的回答）

---

---

### Q3
阅读 `http_conn.h` 中 `read_once()` 的 LT 分支（约第 204-211 行）。当 `m_read_idx_` 达到 `READ_BUFFER_SIZE`（1024）时，会发生什么？请分析原因。

---

（你的回答）

---

---

### Q4
`http_conn.h` 中 `do_request()` 方法（约第 266-273 行）依次调用了 `stat()`、`open()`、`mmap()`。这段代码存在哪些潜在风险？如果请求的静态文件 `./root/index.html` 不存在，会发生什么？

---

（你的回答）

---

---

### Q5
`log/log.cpp` 中 `Log::init()` 创建了一个异步写日志的线程（第 30 行），但 `Log::~Log()` 中没有 `pthread_join` 也没有 `pthread_detach`。这会导致什么问题？`m_queue_` 由谁释放？

---

（你的回答）

---

---

### Q6
`config.cpp` 中 `parse_args()` 的 option string 是 `"p:l:m:o:s:t:c:a:"`，但 switch 语句只处理了 `p`、`l`、`m`、`a`。`webserver.h` 的 `start()` 中端口被硬编码为 `8888`。这两个问题结合会导致什么现象？

---

（你的回答）

---

---

## 二、架构与设计（5 题）

### Q7
这个项目实现了 Reactor 和 Proactor 两种并发模型。请结合 `threadpool.h` 的 `worker()` 和 `webserver.h` 的 `dealwithread()`/`dealwithwrite()`，解释两种模式的核心区别是什么？主线程和 worker 线程的职责分别是什么？

---

（你的回答）

---

---

### Q8
项目使用 `EPOLLONESHOT` 来保证一个 socket 同一时间只被一个线程处理。请解释：
- 为什么需要 `EPOLLONESHOT`？
- 去掉 `EPOLLONESHOT` 后，在 Reactor 模式下可能出现什么问题？

---

（你的回答）

---

---

### Q9
项目使用 `socketpair` 来传递信号（`SIGALRM`、`SIGTERM`），为什么不能直接在信号处理函数里操作 timer 列表？这种"信号 → pipe → epoll"的设计模式叫什么名字，解决了什么问题？

---

（你的回答）

---

---

### Q10
定时器使用了一个按 `expire_` 排序的双向链表（`sort_timer_lst`）。它的 `add_timer()` 时间复杂度是多少？如果服务器有 10000 个并发连接，这种设计的性能瓶颈在哪里？可以怎样优化？

---

（你的回答）

---

---

### Q11
HTTP 响应使用 `writev()` + `mmap()` 的组合来发送数据。相比直接 `write()` 文件内容，这种设计有什么优势？`mmap()` 映射的文件数据在内核态和用户态之间是如何传递的？

---

（你的回答）

---

---

## 三、C++ 语言与并发（4 题）

### Q12
`locker/locker.h` 中 `locker` 类持有 `pthread_mutex_t` 成员，但没有显式删除拷贝构造和拷贝赋值。如果 `locker` 对象被意外拷贝，会导致什么问题？为什么 C++ 的最佳实践要求 RAII 锁类必须 `= delete` 拷贝操作？

---

（你的回答）

---

---

### Q13
`threadpool.h` 的析构函数中，先设置 `shutdown_ = true`，再 `broadcast()`，最后 `pthread_join`。如果去掉 `broadcast()` 只设置 `shutdown_ = true`，worker 线程何时能退出？`broadcast()` 在这里是必需的吗？

---

（你的回答）

---

---

### Q14
`http_conn.h` 的 `init()` 方法（无参版本）会将 `m_linger_` 重置为 `false`。在 HTTP keep-alive 场景下，连接复用后 `m_linger_` 会在什么时候被重新设置为 `true`？keep-alive 能正确工作吗？

---

（你的回答）

---

---

### Q15
`block_queue.h` 中 `push()` 和 `pop()` 都在 `while` 循环中调用 `m_cond_.wait()`。为什么不使用 `if` 而必须使用 `while`？如果改成 `if`，什么场景会出错？

---

（你的回答）

---

---

## 四、网络编程（3 题）

### Q16
`webserver.h` 的 `start()` 中设置了 `SO_REUSEADDR`。这个选项的作用是什么？如果不设置，在压测（频繁重启服务器）时会遇到什么问题？

---

（你的回答）

---

---

### Q17
`http_conn.h` 的 `write()` 方法在 `writev()` 返回 `-1` 且 `errno == EAGAIN` 时，调用 `modfd()` 重新注册 `EPOLLOUT` 然后返回 `true`。为什么不直接 retry 而要让 epoll 再次通知？`writev()` 在什么情况下会返回 `EAGAIN`？

---

（你的回答）

---

---

### Q18
`http_conn.h` 中 `parse_line()` 负责解析 HTTP 请求行。HTTP 协议规定行结束符是 `\r\n`，但代码中单独出现 `\n` 时被判断为 `LINE_BAD`。这种严格校验的优缺点分别是什么？

---

（你的回答）

---

---

## 五、综合开放题（2 题）

### Q19
如果把这个服务器部署到生产环境，你认为最需要优先解决的 3 个问题是什么？（不限于本文列出的 bug）

---

（你的回答）

---

---

### Q20
目前 `do_request()` 只返回 `./root/index.html`，完全忽略了请求的 URL 路径。如果让你实现一个支持多文件的路由机制，你会怎样修改 `http_conn` 的解析和响应流程？简述你的设计思路。

---

（你的回答）

---

---

> 以上共 20 题。请在每道题的"（你的回答）"后填写答案，完成后告诉我，我会逐题评审。