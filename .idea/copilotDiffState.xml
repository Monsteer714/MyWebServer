<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/http_conn (snapshot).h">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/http_conn (snapshot).h" />
              <option name="originalContent" value="//&#10;// Created by Hanhong Wong on 2026/4/20.&#10;//&#10;&#10;#ifndef MYWEBSERVER_HTTP_CONN_H&#10;#define MYWEBSERVER_HTTP_CONN_H&#10;&#10;#include &lt;iostream&gt;&#10;#include &lt;unistd.h&gt;&#10;#include &lt;fstream&gt;&#10;#include &lt;sstream&gt;&#10;#include &lt;string&gt;&#10;#include &lt;cerrno&gt;&#10;&#10;class http_conn {&#10;private:&#10;    int client_fd_;&#10;&#10;public:&#10;    http_conn(int client_fd) {&#10;        client_fd_ = client_fd;&#10;    };&#10;&#10;    ~http_conn() {&#10;        close(client_fd_);&#10;    }&#10;&#10;    std::string read_file(const std::string&amp; path) {&#10;        std::ifstream file(path);&#10;        if (!file.is_open()) {&#10;            return &quot;&quot;;&#10;        }&#10;        std::stringstream ss;&#10;        ss &lt;&lt; file.rdbuf();&#10;        return ss.str();&#10;    }&#10;&#10;    void process() {&#10;        if (client_fd_ &lt; 0) {&#10;            std::cerr &lt;&lt; &quot;Invalid client fd&quot; &lt;&lt; std::endl;&#10;            return;&#10;        }&#10;&#10;        std::cout &lt;&lt; &quot;Client connected&quot; &lt;&lt; std::endl;&#10;&#10;        std::string request;&#10;        char buffer[1024];&#10;        while (true) {&#10;            int n = read(client_fd_, buffer, sizeof(buffer));&#10;            if (n &lt; 0) {&#10;                if (errno == EAGAIN || errno == EWOULDBLOCK) {&#10;                    break; // No more data to read&#10;                } else {&#10;                    perror(&quot;read&quot;);&#10;                    close(client_fd_);&#10;                    return;&#10;                }&#10;            } else if (n == 0) {&#10;                // Client closed the connection&#10;                close(client_fd_);&#10;                return;&#10;            }&#10;            request.append(buffer, n);&#10;            if (request.find(&quot;\r\n\r\n&quot;) != std::string::npos) break;&#10;        }&#10;&#10;        if (request.empty()) {&#10;            std::cerr &lt;&lt; &quot;Empty request received&quot; &lt;&lt; std::endl;&#10;            close(client_fd_);&#10;            return;&#10;        }&#10;&#10;        size_t pos = request.find(&quot;\r\n&quot;);&#10;        if (pos != std::string::npos) {&#10;            std::string first_line = request.substr(0, pos);&#10;            std::istringstream ss(first_line);&#10;            std::string method;&#10;            std::string path;&#10;            std::string version;&#10;            ss &gt;&gt; method &gt;&gt; path &gt;&gt; version;&#10;&#10;            if (path == &quot;/&quot;) {&#10;                path = &quot;/index.html&quot;;&#10;            }&#10;&#10;            if (path.find(&quot;..&quot;) != std::string::npos) {&#10;                std::string resp = &quot;HTTP/1.1 403 Forbidden\r\nContent-Length: 0\r\n\r\n&quot;;&#10;                write(client_fd_, resp.c_str(), resp.length());&#10;                close(client_fd_);&#10;                return;&#10;            }&#10;&#10;            std::string full_path = &quot;./root&quot; + path;&#10;&#10;            std::string body = read_file(full_path);&#10;&#10;            std::string resp = &quot;HTTP/1.1 200 OK\r\n&quot;&#10;                &quot;Content-Type: text/html\r\n&quot;&#10;                &quot;Connection: close\r\n&quot;&#10;                &quot;Content-Length: &quot;&#10;                + std::to_string(body.length()) + &quot;\r\n&quot;&#10;                &quot;\r\n&quot; +&#10;                body;&#10;&#10;            if (body.empty()) {&#10;                resp = &quot;HTTP/1.1 404 Not Found\r\n&quot;&#10;                    &quot;Content-Type: text/html\r\n&quot;&#10;                    &quot;Connection: close\r\n&quot;&#10;                    &quot;Content-Length: 0\r\n&quot;&#10;                    &quot;\r\n&quot;;&#10;            }&#10;&#10;            int bytes_written = 0;&#10;            while (bytes_written &lt; resp.length()) {&#10;                int n = write(client_fd_, resp.c_str() + bytes_written, resp.length() - bytes_written);&#10;                if (n &lt; 0) {&#10;                    if (errno == EAGAIN || errno == EWOULDBLOCK) {&#10;                        continue; // Retry writing&#10;                    } else {&#10;                        perror(&quot;write&quot;);&#10;                        close(client_fd_);&#10;                        return;&#10;                    }&#10;                }&#10;                bytes_written += n;&#10;            }&#10;        }&#10;&#10;        // Ensure all data is flushed before closing the connection&#10;        shutdown(client_fd_, SHUT_WR);&#10;        close(client_fd_);&#10;    }&#10;};&#10;&#10;#endif //MYWEBSERVER_HTTP_CONN_H&#10;" />
              <option name="updatedContent" value="//&#10;// Created by Hanhong Wong on 2026/4/20.&#10;//&#10;&#10;#ifndef MYWEBSERVER_HTTP_CONN_H&#10;#define MYWEBSERVER_HTTP_CONN_H&#10;&#10;#include &lt;iostream&gt;&#10;#include &lt;unistd.h&gt;&#10;#include &lt;fstream&gt;&#10;#include &lt;sstream&gt;&#10;#include &lt;string&gt;&#10;#include &lt;cerrno&gt;&#10;&#10;class http_conn {&#10;private:&#10;    int client_fd_;&#10;&#10;public:&#10;    http_conn(int client_fd) {&#10;        client_fd_ = client_fd;&#10;    };&#10;&#10;    ~http_conn() {&#10;        close(client_fd_);&#10;    }&#10;&#10;    static std::string read_file(const std::string&amp; path) {&#10;        std::ifstream file(path);&#10;        if (!file.is_open()) {&#10;            return &quot;&quot;;&#10;        }&#10;        std::stringstream ss;&#10;        ss &lt;&lt; file.rdbuf();&#10;        return ss.str();&#10;    }&#10;&#10;    void process() {&#10;        if (client_fd_ &lt; 0) {&#10;            std::cerr &lt;&lt; &quot;Invalid client fd&quot; &lt;&lt; std::endl;&#10;            return;&#10;        }&#10;&#10;        std::cout &lt;&lt; &quot;Client connected&quot; &lt;&lt; std::endl;&#10;&#10;        std::string request;&#10;        char buffer[1024];&#10;        while (true) {&#10;            ssize_t n = read(client_fd_, buffer, sizeof(buffer));&#10;            if (n &lt; 0) {&#10;                if (errno == EAGAIN || errno == EWOULDBLOCK) {&#10;                    break; // No more data to read&#10;                } else {&#10;                    perror(&quot;read&quot;);&#10;                    close(client_fd_);&#10;                    return;&#10;                }&#10;            } else if (n == 0) {&#10;                // Client closed the connection&#10;                close(client_fd_);&#10;                return;&#10;            }&#10;            request.append(buffer, n);&#10;            if (request.find(&quot;\r\n\r\n&quot;) != std::string::npos) break;&#10;        }&#10;&#10;        if (request.empty()) {&#10;            std::cerr &lt;&lt; &quot;Empty request received&quot; &lt;&lt; std::endl;&#10;            close(client_fd_);&#10;            return;&#10;        }&#10;&#10;        size_t pos = request.find(&quot;\r\n&quot;);&#10;        if (pos != std::string::npos) {&#10;            std::string first_line = request.substr(0, pos);&#10;            std::istringstream ss(first_line);&#10;            std::string method;&#10;            std::string path;&#10;            std::string version;&#10;            ss &gt;&gt; method &gt;&gt; path &gt;&gt; version;&#10;&#10;            if (path == &quot;/&quot;) {&#10;                path = &quot;/index.html&quot;;&#10;            }&#10;&#10;            if (path.find(&quot;..&quot;) != std::string::npos) {&#10;                std::string resp = &quot;HTTP/1.1 403 Forbidden\r\nContent-Length: 0\r\n\r\n&quot;;&#10;                write(client_fd_, resp.c_str(), resp.length());&#10;                close(client_fd_);&#10;                return;&#10;            }&#10;&#10;            std::string full_path = &quot;./root&quot; + path;&#10;&#10;            std::string body = read_file(full_path);&#10;&#10;            std::string resp = &quot;HTTP/1.1 200 OK\r\n&quot;&#10;                &quot;Content-Type: text/html\r\n&quot;&#10;                &quot;Connection: close\r\n&quot;&#10;                &quot;Content-Length: &quot;&#10;                + std::to_string(body.length()) + &quot;\r\n&quot;&#10;                &quot;\r\n&quot; +&#10;                body;&#10;&#10;            if (body.empty()) {&#10;                resp = &quot;HTTP/1.1 404 Not Found\r\n&quot;&#10;                    &quot;Content-Type: text/html\r\n&quot;&#10;                    &quot;Connection: close\r\n&quot;&#10;                    &quot;Content-Length: 0\r\n&quot;&#10;                    &quot;\r\n&quot;;&#10;            }&#10;&#10;            ssize_t bytes_written = 0;&#10;            while (bytes_written &lt; resp.length()) {&#10;                ssize_t n = write(client_fd_, resp.c_str() + bytes_written, resp.length() - bytes_written);&#10;                if (n &lt; 0) {&#10;                    if (errno == EAGAIN || errno == EWOULDBLOCK) {&#10;                        continue; // Retry writing&#10;                    } else {&#10;                        perror(&quot;write&quot;);&#10;                        close(client_fd_);&#10;                        return;&#10;                    }&#10;                }&#10;                bytes_written += n;&#10;            }&#10;        }&#10;&#10;        // Ensure all data is flushed before closing the connection&#10;        shutdown(client_fd_, SHUT_WR);&#10;        close(client_fd_);&#10;    }&#10;};&#10;&#10;#endif //MYWEBSERVER_HTTP_CONN_H" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>